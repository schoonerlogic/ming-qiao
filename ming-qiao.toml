# Project identifier for NATS subject routing
project = "mingqiao"

mode = "passive"
data_dir = "data"
port = 7777

[nats]
enabled = true
url = "nats://localhost:4222"

[database]
url = "ws://localhost:8000"
username = "root"
password = "root"

# Laozi-Jung: real-time event stream for witnessing
[[watchers]]
agent = "laozi-jung"
role = "observer"
subjects = ["am.events.mingqiao"]

[watchers.action]
type = "file_append"
path = "/Users/proteus/astralmaris/echoessence/merlin/observations/stream.jsonl"
