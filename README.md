# Proteus — Operator Worktree

Hands-on coordination layer for AstralMaris agent management.
This worktree does not contain application code.

## Structure
- `docs/` — architecture decisions, coordination protocols
- `instructions/` — agent task briefs, context packages
- `scripts/` — sync, health checks, worktree management

## Branch
`ops/proteus` — operator branch, does not merge into develop.
